apiVersion: apps/v1
kind: Deployment
metadata:
  name: llm-adapter
  namespace: amael-ia # O el namespace que uses
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llm-adapter
  template:
    metadata:
      labels:
        app: llm-adapter
    spec:
      serviceAccountName: llm-adapter-sa
      containers:
      - name: adapter
        image: registry.richardx.dev/llm-adapter:1.0.7
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        env:
        - name: VAULT_ADDR
          value: "http://vault.vault.svc.cluster.local:8200"
        - name: VAULT_ROLE
          value: "llm-adapter"
        - name: OLLAMA_BASE_URL
          # IMPORTANTE: Esta es la URL interna del cluster de tu Ollama
          value: "http://ollama-service.amael-ia.svc.cluster.local:11434" 
        - name: DEFAULT_MODEL
          value: "glm4" # Nombre exacto del modelo en Ollama
---
apiVersion: v1
kind: Service
metadata:
  name: llm-adapter-service
  namespace: amael-ia
spec:
  selector:
    app: llm-adapter
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000